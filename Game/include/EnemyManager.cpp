#include "EnemyManager.h"

namespace gnGame {

	/*
	EnemyList EnemyManager::enemyList{};

	void EnemyManager::addActor(UniquePtr<Enemy>&& _actor)
	{
		enemyList.push_back(_actor);
	}

	void EnemyManager::removeActor(UniquePtr<Enemy> _actor)
	{
	}

	void EnemyManager::onStartActorList()
	{
		for (auto& enemy : enemyList) {
			enemy->onStart();
		}
	}

	void EnemyManager::setMap(Map& _map)
	{
		for (auto& enemy : enemyList) {
			enemy->setMap(_map);
		}
	}

	void EnemyManager::onUpdateActorList()
	{
		float i{ 1.82 };
		for ( auto& enemy : enemyList) {
			enemy->onUpdate(i);
			i += 0.46f;
		}
	}
	*/
}
